# Screenshots

Empty for now — this project is a CLI + backend API with no UI. If you add
screenshots later, good candidates:

- A terminal capture of `python main.py` — a short back-and-forth showing
  guardrail rejection, a greeting, and a knowledge question.
- The FastAPI auto-generated docs UI at `http://localhost:8001/docs` (once
  `uvicorn src.api:app` is running) showing `/query` and `/reload`.
- A `src/evaluation/results/*.csv` run summarized as a table/chart.
