[project]
name = "intelligent-document-ai"
version = "0.2.0"
description = "LangGraph-orchestrated RAG chatbot: guardrails -> intent -> retrieval -> rerank -> generation -> memory."
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
